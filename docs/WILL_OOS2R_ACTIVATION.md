# OOS-2R first-start preparation — NOT STARTED

Base: `8f76fd8442dd9589714dde6a9a81e5d656252fa6`. The approved freeze is unchanged:

- protocolId: `will-edge-gate-oos2r-v1`
- campaignId: `will-edge-gate-oos2r-20260918-v1`
- edgeCut: `2026-09-18T22:49:33.233Z`; strictly later OPEN only
- threshold/operator: `0.599936` / `<=`
- baselineCount: 1095
- freeze SHA256: `f61df0aecdd9336cdd3df52b51dbb32fd74094ed680a85ff4d2490e5ac101892`

## Chosen paths and configuration

History: `C:/Users/skdri/will-trader-commissioning/backend/data/will-history.json`.
New evidence directory: `C:/Users/skdri/will-trader-commissioning/backend/data/oos2r-evidence-20260918-v1`.
Managed evidence scan root: `C:/Users/skdri/will-trader-commissioning/backend/data` (recursive).

The evidence path must NOT yet exist, even as an empty directory. Preparation leaves it absent. Future guarded startup creates it exclusively, empty, with non-recursive mkdir; EEXIST blocks competing starts/reuse. Merely constructing the runtime creates no WAL, manifest, cycle or record. No directory was initialized for the real campaign during this mission.

`backend/config/oos2r-activation.env.example` is a review template, not loaded by dotenv. No real environment file or process environment was changed. It records the exact identities, history/evidence paths and JSON array of scan roots, and requires literal `WILL_OOS2R_START_AUTHORIZATION=START_APPROVED_OOS2R_FIRST_COLLECTION_V1`. It keeps `WILL_PAPER_MONITOR_ENABLED=false`. Only a separate operator START authorization may install this configuration and enable the monitor. Do not start the server with the template as a dry run: runtime construction reserves the new directory. Do not put credentials in this template.

The operator must deploy this review commit (or its approved integration) before attempting the first start. No merge or deployment is performed here. Existing provider configuration remains independent; the gate does not grant broker capability or change decisions.

## Fail-closed boundary

`inspectOos2rActivation()` reads the pinned freeze, history IDs and membership fields, and scans known evidence roots. It does not start services, hydrate credentials, create files or inspect outcome performance. It requires:

1. Exact approved freeze bytes and identities.
2. Exactly 1095 unique current history IDs with the frozen canonical ID hash. This conservative first-start policy also rejects extra IDs; such a change requires review, not selecting/reconstructing a subset. Hashes are generated by the unchanged canonical algorithm in the protocol.
3. No existing history record with either the OOS-2R protocol or campaign membership.
4. No existing target directory and no matching OOS-2R manifest/WAL under the declared scan roots. Unreadable evidence or symlinks in the scan scope block. At least the entire history data directory must be covered.
5. Clock strictly after edgeCut.

The scan is scoped, not a claim about every disk/backup or unregistered external directory. If the operator has used other evidence storage roots, add them to WILL_OOS2R_EVIDENCE_SCAN_ROOTS and rerun preflight before activation. A missing root fails closed. No old journal is removed, renamed, repaired or reset.

The server runs this preflight before secret hydration, history backup, provider objects and monitor creation. It reruns preflight before exclusive evidence-directory creation, checks the loaded history snapshot, and rechecks IDs/membership before the first actual OPEN. The clock supplied to the authoritative runtime is checked on every OPEN/seal. Any inconsistency throws/pauses; no legacy fallback for an attempted OOS-2R activation.

The existing internal capability, writer registration, WAL-before-history, generation and seal invariants remain unchanged. First real cycle OPEN is issued only by the existing scheduler through this guarded runtime; it carries the exact identities, cycleId, positive writerGeneration and checked openedAt. No artificial real campaign cycle is used to test readiness.

This is a FIRST-START gate, not a restart/recovery authorization. Once its directory exists, another startup with this activation configuration is blocked. Existing OPEN/INVALID/stale-lock and recovery policy still requires separate operator review; never choose a new directory to conceal a started campaign. Storage/power-loss guarantees are unchanged.

## Tests and restrictions

Activation tests use synthetic two-ID histories, temporary directories and explicit test-only freeze-loader injection. The environment adapter never accepts a loader override. Real startup always verifies the pinned freeze. Tests cover mismatching/missing/duplicate/extra IDs, existing membership/evidence, tampered freeze, exact authorization, edgeCut equality/regression, exclusive directory reservation and first authoritative synthetic OPEN, and mutation before first OPEN.

No private baseline IDs are committed. No existing history, freeze, evaluator, threshold, operator, Champion, dashboard, broker or real campaign record is modified. No backend/PAPER/provider session is started during preparation.
