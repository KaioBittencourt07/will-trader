export const LIFECYCLE_VERSION='signal-lifecycle-shadow-v1';
export function assessSignalLifecycle({snapshot={},decision={},now=Date.now()}={}){const reasons=[];let state='DETECTED';if(snapshot.valid===false||snapshot.status==='STALE'){state='UNKNOWN';reasons.push('DATA_UNKNOWN')}else if(decision.timingStatus==='EXPIRED'){state='EXPIRED';reasons.push('TIMING_EXPIRED')}else if(decision.timingStatus==='WAIT_TIMING'){state='WAIT_RETEST';reasons.push('TIMING_WAIT')}else if(decision.executable){state='ENTRY_READY';reasons.push('CHAMPION_READY')}else if(decision.setup&&decision.setup!=='UNKNOWN'){state='FORMING';reasons.push('SETUP_FORMING')}const decay=Math.min(100,(Number(snapshot.exhaustion)||0)*25+((Number(snapshot.rangeExpansion)||0)>1.8?30:0));if(decay>=50&&state==='ENTRY_READY'){state='DECAYING';reasons.push('DECAY_SHADOW')}return {mode:'SHADOW',lifecycleVersion:LIFECYCLE_VERSION,state,decayScore:decay,evidence:reasons,observedAt:new Date(now).toISOString()};}
