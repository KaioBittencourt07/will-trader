import assert from 'node:assert/strict'; import test from 'node:test'; import { assessDecisionRobustness, ROBUSTNESS_VERSION } from '../engine/src/robustness.js';
const snapshot={asset:'EUR/USD',timeframe:'1min',timestamp:new Date().toISOString(),price:1.1,trend:.8,momentum:.7,structure:.7,volatility:.2,confirmations:4,candleCount:50,valid:true};
test('robustness is deterministic, offline and does not mutate champion input',()=>{const decision={direction:'BUY',setup:'CONTINUATION',blocked:false};const a=assessDecisionRobustness({snapshot,decision}),b=assessDecisionRobustness({snapshot,decision});assert.deepEqual(a,b);assert.equal(a.mode,'SHADOW');assert.equal(a.robustnessVersion,ROBUSTNESS_VERSION);assert.deepEqual(decision,{direction:'BUY',setup:'CONTINUATION',blocked:false});});
test('stale or missing evidence is UNKNOWN rather than a fabricated score',()=>{assert.equal(assessDecisionRobustness({snapshot:{valid:false}}).status,'UNKNOWN');assert.equal(assessDecisionRobustness({snapshot:{timestamp:new Date().toISOString(),price:null}}).status,'UNKNOWN');});
