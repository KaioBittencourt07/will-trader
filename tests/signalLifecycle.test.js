import assert from 'node:assert/strict';import test from 'node:test';import {assessSignalLifecycle} from '../engine/src/signalLifecycle.js';test('lifecycle is deterministic shadow and does not mutate champion',()=>{const d={executable:true,timingStatus:'READY'};const x=assessSignalLifecycle({snapshot:{valid:true,exhaustion:3},decision:d,now:0});assert.equal(x.mode,'SHADOW');assert.equal(x.state,'DECAYING');assert.deepEqual(d,{executable:true,timingStatus:'READY'});});
